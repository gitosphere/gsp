#compdef gsp

__gsp_complete() {
    local -ar non_empty_completions=("${@:#(|:*)}")
    local -ar empty_completions=("${(M)@:#(|:*)}")
    _describe -V '' non_empty_completions -- empty_completions -P $'\'\''
}

__gsp_custom_complete() {
    local -a completions
    completions=("${(@f)"$("${command_name}" "${@}" "${command_line[@]}")"}")
    if [[ "${#completions[@]}" -gt 1 ]]; then
        __gsp_complete "${completions[@]:0:-1}"
    fi
}

__gsp_cursor_index_in_current_word() {
    if [[ -z "${QIPREFIX}${IPREFIX}${PREFIX}" ]]; then
        printf 0
    else
        printf %s "${#${(z)LBUFFER}[-1]}"
    fi
}

_gsp() {
    emulate -RL zsh -G
    setopt extendedglob nullglob numericglobsort
    unsetopt aliases banghist

    local -xr SAP_SHELL=zsh
    local -x SAP_SHELL_VERSION
    SAP_SHELL_VERSION="$(builtin emulate zsh -c 'printf %s "${ZSH_VERSION}"')"
    local -r SAP_SHELL_VERSION

    local context state state_descr line
    local -A opt_args

    local -r command_name="${words[1]}"
    local -ar command_line=("${words[@]}")
    local -ir current_word_index="$((CURRENT - 1))"

    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'auth:Manage Gitosphere authentication'
            'org:Manage Gitosphere Organizations'
            'member:Manage Gitosphere membership independently of Space membership'
            'repo:Manage Gitosphere repositories'
            'space:Manage Permissioned Data Spaces'
            'traffic:Inspect admission limits and pause or resume new requests'
            'quota:Inspect usage and manage service quotas'
            'invite:Manage service invitations'
            'access:Inspect Gitosphere authorization decisions'
            'audit:Inspect secret-safe Gitosphere audit events'
            'issue:Manage issues in a Gitosphere repository'
            'pr:Manage pull requests in a Gitosphere repository'
            'completion:Generate a shell completion script'
            'help:Show subcommand help information.'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        auth|org|member|repo|space|traffic|quota|invite|access|audit|issue|pr|completion|help)
            "_gsp_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_auth() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'login:Authorize and store a DPoP-bound Gitosphere session'
            'status:Show non-secret authentication status'
            'logout:Delete the selected profile'\''s session'
            'use:Select the default profile without changing its credentials'
            'spaces-alpha-smoke:Run a destructive, self-cleaning smoke test against a Spaces Alpha PDS'
            'spaces-access-smoke:Verify direct PDS read/write permissions in temporary Spaces'
            'permission-set-probe:Verify that a PDS accepts an external OAuth permission set during PAR'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        login|status|logout|use|spaces-alpha-smoke|spaces-access-smoke|permission-set-probe)
            "_gsp_auth_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_auth_login() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        ':account:'
        '--pds[PDS origin override used for AT Protocol OAuth]:pds:'
        '--no-browser[Print the authorization URL instead of opening a browser]'
        '--callback-port[Fixed OAuth callback port (1...65535)]:callback-port:'
        '--permission-set[OAuth Permission Set NSID]:permission-set:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_auth_status() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_auth_logout() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_auth_use() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        ':profile:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_auth_spaces-alpha-smoke() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--pds[Spaces Alpha PDS origin]:pds:'
        '--did-document-origin[Loopback fixture DID document origin for local development]:did-document-origin:'
        '--contract[Permission contract\: bulletin, gitosphere, or gitosphere-test]:contract:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_auth_spaces-access-smoke() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--member-profile[Signed-in non-owner profile used for direct PDS operations]:member-profile:'
        '--yes[Confirm temporary Space and record writes]'
        '--dry-run[Validate profiles without refreshing sessions or contacting the PDS]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_auth_permission-set-probe() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--pds[PDS origin used for AT Protocol OAuth]:pds:'
        '--permission-set[Permission-set Lexicon NSID]:permission-set:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_org() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'create:'
            'get:'
            'list:'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        create|get|list)
            "_gsp_org_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_org_create() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a create request]:idempotency-key:'
        '--name[Organization public name]:name:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_org_get() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--organization[Organization public name]:organization:'
        '--organization-id[Immutable Organization ID]:organization-id:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_org_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--cursor[Opaque cursor returned by a previous list request]:cursor:'
        '--limit[Maximum number of resources to return (1...100)]:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_member() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'add:'
            'list:'
            'update:'
            'remove:'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        add|list|update|remove)
            "_gsp_member_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_member_add() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--organization[Organization public name]:organization:'
        '--organization-id[Immutable Organization ID]:organization-id:'
        '(-R --repo)'{-R,--repo}'[Repository using OWNER/NAME]:repo:'
        '--repository-id[Immutable Repository ID]:repository-id:'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '--principal[Principal DID]:principal:'
        '--role[Gitosphere role\: owner, admin, writer, or reader]:role:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_member_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--organization[Organization public name]:organization:'
        '--organization-id[Immutable Organization ID]:organization-id:'
        '(-R --repo)'{-R,--repo}'[Repository using OWNER/NAME]:repo:'
        '--repository-id[Immutable Repository ID]:repository-id:'
        '--cursor[Opaque cursor returned by a previous list request]:cursor:'
        '--limit[Maximum number of resources to return (1...100)]:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_member_update() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--organization[Organization public name]:organization:'
        '--organization-id[Immutable Organization ID]:organization-id:'
        '(-R --repo)'{-R,--repo}'[Repository using OWNER/NAME]:repo:'
        '--repository-id[Immutable Repository ID]:repository-id:'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '--principal[Principal DID]:principal:'
        '--role[New Gitosphere role\: owner, admin, writer, or reader]:role:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_member_remove() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--organization[Organization public name]:organization:'
        '--organization-id[Immutable Organization ID]:organization-id:'
        '(-R --repo)'{-R,--repo}'[Repository using OWNER/NAME]:repo:'
        '--repository-id[Immutable Repository ID]:repository-id:'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '--principal[Principal DID]:principal:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'create:Create a repository and publish its AT Protocol record'
            'view:'
            'list:'
            'search:Search public repositories using the selected profile when configured'
            'deactivate:'
            'clone:Clone a Gitosphere repository with ephemeral Git grants'
            'remote:Manage Gitosphere Git remotes'
            'tree:List files in a repository tree'
            'blob:Show a file from a repository'
            'diff:Display the unified diff between two exact commits'
            'repair-space-policy:Set a private repository Space to the current Gitosphere managing-app service'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        create|view|list|search|deactivate|clone|remote|tree|blob|diff|repair-space-policy)
            "_gsp_repo_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_repo_create() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a create request]:idempotency-key:'
        '--public[Publish repository metadata to the account PDS]'
        '--internal[Store repository metadata in a Space]'
        '--private[Store repository metadata in a private Space]'
        ':name:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_view() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        ':repository:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--cursor[Opaque pagination cursor]:cursor:'
        '--limit[Maximum results (1...100)]:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_search() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--cursor[Opaque pagination cursor]:cursor:'
        '--limit[Maximum results (1...100)]:limit:'
        ':query:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_deactivate() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a create request]:idempotency-key:'
        ':repository:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_clone() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        ':repository:'
        ':directory:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_remote() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'set:Configure a repository-local Gitosphere remote'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        set)
            "_gsp_repo_remote_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_repo_remote_set() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--remote[Git remote name]:remote:'
        '--force[Replace conflicting remote or credential-helper configuration]'
        ':repository:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_tree() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--ref[Git branch, tag, or reachable commit]:ref:'
        '--path[Directory path within the repository]:path:'
        ':repository:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_blob() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--ref[Git branch, tag, or reachable commit]:ref:'
        ':path:'
        ':repository:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_diff() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        ':base:'
        ':head:'
        '--repository[Immutable Repository ID or OWNER/NAME (defaults to the current Git repository)]:repository:'
        '--no-pager[Write directly without an external diff viewer or pager]'
        '--color[Color mode for an external viewer\: auto, always, or never]:color:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_repo_repair-space-policy() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        '--dry-run[Describe the update without applying it]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_space() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'member:Manage Space membership independently of Gitosphere membership'
            'delete:Delete a disposable Permissioned Data Space'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        member|delete)
            "_gsp_space_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_space_member() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'put:Set both read and write permissions for a Space member'
            'add:Replaced by member put with explicit read/write permissions'
            'list:'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        put|add|list)
            "_gsp_space_member_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_space_member_put() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--space[Permissioned Data Space URI]:space:'
        '--did[Member account DID]:did:'
        '--read[Allow read access (true or false); required]:read:'
        '--write[Allow write notifications (true or false); required]:write:'
        '--yes[Confirm replacing both member permissions]'
        '--dry-run[Validate the request without contacting the PDS]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_space_member_add() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--space[Permissioned Data Space URI]:space:'
        '--did[Member account DID]:did:'
        '--yes[Confirm the membership change]'
        '--dry-run[Validate the request without contacting the PDS]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_space_member_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--space[Permissioned Data Space URI]:space:'
        '--cursor[Opaque cursor returned by a previous list request]:cursor:'
        '--limit[Maximum number of members to return (1...1000)]:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_space_delete() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--space[Permissioned Data Space URI; must be a disposable verification Space]:space:'
        '--verification-space[Exact verification Space URI allowlisted for deletion]:verification-space:'
        '--yes[Confirm deletion]'
        '--dry-run[Validate the target without contacting the PDS]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_traffic() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'status:'
            'pause:'
            'resume:'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        status|pause|resume)
            "_gsp_traffic_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_traffic_status() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_traffic_pause() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '*--target[Repeat for auth, invite, api-write, git-read or git-write]:target:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_traffic_resume() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '*--target[Repeat for auth, invite, api-write, git-read or git-write]:target:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_quota() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'usage:'
            'show:'
            'list:'
            'set:'
            'reset:'
            'reconcile:'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        usage|show|list|set|reset|reconcile)
            "_gsp_quota_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_quota_usage() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--scope[Quota scope\: did, organization, repository; show/set/reset also accept default]:scope:'
        '--subject[Subject DID or UUID; for default, use did, organization or repository]:subject:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_quota_show() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--scope[Quota scope\: did, organization, repository; show/set/reset also accept default]:scope:'
        '--subject[Subject DID or UUID; for default, use did, organization or repository]:subject:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_quota_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--cursor[Opaque cursor returned by a previous list request]:cursor:'
        '--limit[Maximum number of resources to return (1...100)]:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_quota_set() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--scope[Quota scope\: did, organization, repository; show/set/reset also accept default]:scope:'
        '--subject[Subject DID or UUID; for default, use did, organization or repository]:subject:'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '--repository-limit[Maximum retained Repository count]:repository-limit:'
        '--byte-limit[Maximum stored Git bytes]:byte-limit:'
        '--push-limit[Maximum bytes in one push pack]:push-limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_quota_reset() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--scope[Quota scope\: did, organization, repository; show/set/reset also accept default]:scope:'
        '--subject[Subject DID or UUID; for default, use did, organization or repository]:subject:'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_quota_reconcile() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--scope[Quota scope\: did, organization, repository; show/set/reset also accept default]:scope:'
        '--subject[Subject DID or UUID; for default, use did, organization or repository]:subject:'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_invite() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'create:Issue a code, displayed only in the first successful response'
            'list:List invitation metadata without codes'
            'revoke:'
            'redeem:Redeem a code for the authenticated DID'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        create|list|revoke|redeem)
            "_gsp_invite_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_invite_create() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '--max-uses[Maximum distinct users (1...10000)]:max-uses:'
        '--expires-in-seconds[Lifetime in seconds (1...31536000; default 7 days)]:expires-in-seconds:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_invite_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--cursor[Opaque cursor returned by a previous list request]:cursor:'
        '--limit[Maximum number of resources to return (1...100)]:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_invite_revoke() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        ':invite-id:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_invite_redeem() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        '--code-file[Read the code from a file; use - for stdin. Otherwise prompt without echo]:code-file:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_access() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'explain:Explain the Server authorization decision for a principal and target'
            'status:Show your service admission status'
            'list:List service admissions (service administrator only)'
            'grant:Grant service access for a DID (service administrator only)'
            'suspend:Suspend service access for a DID (service administrator only)'
            'resume:Resume service access for a DID (service administrator only)'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        explain|status|list|grant|suspend|resume)
            "_gsp_access_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_access_explain() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar ___kind=('organization' 'project' 'repository' 'space' 'record')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--kind[Target kind\: organization, project, repository, space, or record]:kind:{__gsp_complete "${___kind[@]}"}'
        '--organization[Organization public name]:organization:'
        '--organization-id[Immutable Organization ID]:organization-id:'
        '--project[Project public name]:project:'
        '--project-id[Immutable Project ID]:project-id:'
        '--repository-id[Immutable repository ID]:repository-id:'
        '--space-id[Permissioned Data Space reference]:space-id:'
        '--ref[Optional Git ref for a repository target]:ref:'
        '--collection[Permissioned record collection NSID]:collection:'
        '--record-uri[Optional authoritative Permissioned record AT URI]:record-uri:'
        '--principal[Principal DID whose access is evaluated]:principal:'
        '--action[Semantic action evaluated by the Server]:action:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_access_status() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_access_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--cursor[Opaque cursor returned by a previous list request]:cursor:'
        '--limit[Maximum number of resources to return (1...100)]:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_access_grant() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        ':did:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_access_suspend() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        ':did:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_access_resume() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for a mutation request]:idempotency-key:'
        ':did:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_audit() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'list:'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        list)
            "_gsp_audit_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_audit_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '--cursor[Opaque cursor returned by a previous list request]:cursor:'
        '--limit[Maximum number of resources to return (1...100)]:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_issue() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'create:'
            'list:'
            'view:'
            'edit:'
            'close:'
            'reopen:'
            'adopt-baseline:Review and adopt existing Issue records before enabling authorized mutations'
            'resume-mutation:Resume publishing and committing a prepared Issue mutation'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        create|list|view|edit|close|reopen|adopt-baseline|resume-mutation)
            "_gsp_issue_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_issue_create() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        '--dry-run[Describe the operation without applying it]'
        '--idempotency-key[Idempotency key for safe retries; generated when omitted]:idempotency-key:'
        '(-t --title)'{-t,--title}'[Issue title]:title:'
        '(-b --body)'{-b,--body}'[Issue body]:body:'
        '(-F --body-file)'{-F,--body-file}'[Read the issue body from a file; use - for stdin]:body-file:'
        '(-e --editor)'{-e,--editor}'[Open an editor; the first line is the title and the remaining text is the body]'
        '--parent[Set the parent issue after creation]:parent:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_issue_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar ___state=('open' 'closed' 'all')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        '--cursor[Opaque cursor returned by a previous list request]:cursor:'
        '--limit[Maximum number of resources to return (1...100)]:limit:'
        '--skip[Number of resources to skip before returning results]:skip:'
        '--max-count[Maximum number of resources to return across pages]:max-count:'
        '--state[Filter issues by state (open, closed, or all)]:state:{__gsp_complete "${___state[@]}"}'
        '--parent[Filter by parent issue number]:parent:'
        '--related[Filter by related issue number]:related:'
        '--blocking[Filter by an issue this issue blocks]:blocking:'
        '--blocked-by[Filter by an issue blocking this issue]:blocked-by:'
        '--has-children[Filter to issues with children]'
        '--blocked[Filter to issues with open blockers]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_issue_view() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':issue:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_issue_edit() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':issue:'
        '--title[Set the issue title]:title:'
        '(-b --body)'{-b,--body}'[Set the issue body]:body:'
        '(-F --body-file)'{-F,--body-file}'[Read the issue body from a file; use - for stdin]:body-file:'
        '--editor[Open the configured editor to edit the title and body]'
        '--parent[Set the parent issue]:parent:'
        '--remove-parent[Remove the current parent]'
        '--add-sub-issue[Add a child issue]:add-sub-issue:'
        '--remove-sub-issue[Remove a child issue]:remove-sub-issue:'
        '--add-related[Add a related issue]:add-related:'
        '--remove-related[Remove a related issue]:remove-related:'
        '--add-blocking[Mark this issue as blocking another issue]:add-blocking:'
        '--remove-blocking[Remove an issue blocked by this issue]:remove-blocking:'
        '--add-blocked-by[Mark this issue as blocked by another issue]:add-blocked-by:'
        '--remove-blocked-by[Remove an issue blocking this issue]:remove-blocked-by:'
        '--idempotency-key[Idempotency key; required with --non-interactive]:idempotency-key:'
        '--dry-run[Describe the operation without applying it]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_issue_close() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':issue:'
        '--idempotency-key[Idempotency key for safe retries; generated when omitted]:idempotency-key:'
        '--dry-run[Describe the operation without applying it]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_issue_reopen() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':issue:'
        '--idempotency-key[Idempotency key for safe retries; generated when omitted]:idempotency-key:'
        '--dry-run[Describe the operation without applying it]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_issue_adopt-baseline() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        '--dry-run'
        '--source-digest[Source digest returned by the dry run]:source-digest:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_issue_resume-mutation() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':operation-id:'
        '--dry-run'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr() {
    local -i ret=1
    local -ar arg_specs=(
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
        '(-): :->command'
        '(-)*:: :->arg'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0
    case "${state}" in
    command)
        local -ar subcommands=(
            'create:'
            'list:'
            'view:'
            'edit:'
            'diff:Display the unified diff for a pull request round'
            'comment:'
            'comments:'
            'review:'
            'status:'
            'merge:'
        )
        _describe -V subcommand subcommands && ret=0
        ;;
    arg)
        case "${words[1]}" in
        create|list|view|edit|diff|comment|comments|review|status|merge)
            "_gsp_pr_${words[1]}" && ret=0
            ;;
        esac
        ;;
    esac

    return "${ret}"
}

_gsp_pr_create() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        '(-t --title)'{-t,--title}'[Pull request title]:title:'
        '(-b --body)'{-b,--body}'[Pull request body]:body:'
        '(-F --body-file)'{-F,--body-file}'[Read the pull request body from a file; use - for stdin]:body-file:'
        '--base[Base branch; defaults to the repository default branch]:base:'
        '--head[Head branch; defaults to the current local branch]:head:'
        '--idempotency-key[Idempotency key for safe retries; generated when omitted]:idempotency-key:'
        '--dry-run[Describe the operation without applying it]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr_list() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        '--state[Filter by state\: open, closed, merged, or all]:state:'
        '--author[Filter by author DID]:author:'
        '--cursor[Opaque cursor returned by a previous list request]:cursor:'
        '--limit[Maximum number of pull requests to return (1...100)]:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr_view() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':number:'
        '--no-pager[Write directly without an external pager]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr_edit() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':number:'
        '--title[Set the pull request title]:title:'
        '(-b --body)'{-b,--body}'[Set the pull request body]:body:'
        '(-F --body-file)'{-F,--body-file}'[Read the pull request body from a file; use - for stdin]:body-file:'
        '--editor[Open the configured editor to edit the title and body]'
        '--idempotency-key[Idempotency key for safe retries; generated when omitted]:idempotency-key:'
        '--dry-run[Describe the operation without applying it]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr_diff() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':number:'
        '--round[Zero-based immutable pull request round; defaults to the current round]:round:'
        '--no-pager[Write directly without an external diff viewer or pager]'
        '--color[Color mode for an external viewer\: auto, always, or never]:color:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr_comment() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':number:'
        '(-b --body)'{-b,--body}':body:'
        '(-F --body-file)'{-F,--body-file}'[Read body from a file; use - for stdin]:body-file:'
        '--round:round:'
        '--path:path:'
        '--side[Diff side\: base or head]:side:'
        '--start-line:start-line:'
        '--line:line:'
        '--severity:severity:'
        '--category:category:'
        '--blocking'
        '--idempotency-key:idempotency-key:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr_comments() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':number:'
        '--round:round:'
        '--cursor:cursor:'
        '--limit:limit:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr_review() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':number:'
        '--comment'
        '--approve'
        '--request-changes'
        '--round:round:'
        '(-b --body)'{-b,--body}':body:'
        '(-F --body-file)'{-F,--body-file}'[Read body from a file; use - for stdin]:body-file:'
        '--evidence-file[JSON file containing verification and unverified arrays]:evidence-file:'
        '--unverified[Scope not verified; repeatable]:unverified:'
        '--idempotency-key:idempotency-key:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr_status() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':number:'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_pr_merge() {
    local -i ret=1
    local -ar ___session_store=('keychain' 'file')
    local -ar arg_specs=(
        '--json[Emit stable machine-readable JSON]'
        '--verbose[Emit diagnostic details]'
        '--non-interactive[Disable all interactive prompts]'
        '--profile[Select a configured profile]:profile:'
        '--timeout-seconds[Request timeout in seconds]:timeout-seconds:'
        '--session-store[Session storage\: keychain or file]:session-store:{__gsp_complete "${___session_store[@]}"}'
        '(-R --repo)'{-R,--repo}'[Select a repository using OWNER/NAME]:repo:'
        ':number:'
        '--match-head-commit[Full current head commit SHA reviewed for this merge]:match-head-commit:'
        '--idempotency-key[Idempotency key for safe retries; generated when omitted]:idempotency-key:'
        '--dry-run[Validate the merge without updating refs or Pull Request state]'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_completion() {
    local -i ret=1
    local -ar _shell=('bash' 'zsh' 'fish')
    local -ar arg_specs=(
        ':shell:{__gsp_complete "${_shell[@]}"}'
        '--version[Show the version.]'
        '(-h --help)'{-h,--help}'[Show help information.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

_gsp_help() {
    local -i ret=1
    local -ar arg_specs=(
        '*:subcommands:'
        '--version[Show the version.]'
    )
    _arguments -w -s -S : "${arg_specs[@]}" && ret=0

    return "${ret}"
}

if [[ "${funcstack[1]}" = _gsp ]]; then
    _gsp "${@}"
else
    compdef _gsp gsp
fi